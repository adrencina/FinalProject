<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProject"
        tools:targetApi="31">
        <activity
            android:name=".ui.buy.BuyActivity"
            android:exported="false" />
        <activity
            android:name=".ui.leftbar.presenter.LeftBarActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.presenter.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.presenter.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.register.presenter.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.search.presenter.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".ui.preLogin.presenter.PreLoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>